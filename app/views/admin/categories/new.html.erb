<h1>New category</h1>

<%= error_messages_for :category %>

<% form_for([:admin,@category]) do |f| %>

  <p>
    <b>Name</b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', admin_categories_path %>
